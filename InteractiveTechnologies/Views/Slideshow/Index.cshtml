@model IEnumerable<InteractiveTechnologies.Models.Slideshow>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.SlideLink)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Image.ImageName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SlideOrder)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SlideLink)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Image.ImageName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SlideOrder)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.SlideID }) |
            @Html.ActionLink("Details", "Details", new { id = item.SlideID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.SlideID })
        </td>
    </tr>
}

</table>
